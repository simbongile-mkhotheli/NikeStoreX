export const ROUTES = {
    HOME: '/',
    CART: '/cart',
    PRODUCT: '/product',
    PRODUCTS: '/products',
    CHECKOUT: '/checkout',
    PROFILE: '/profile',
} as const;

export const API_ENDPOINTS = {
    PRODUCTS: '/api/products',
    CART: '/api/cart',
    USER: '/api/user',
    ORDERS: '/api/orders',
} as const;
